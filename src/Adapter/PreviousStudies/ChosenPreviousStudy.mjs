/**
 * @typedef {{"certificate-type": string, "start-date": number, "end-date": number, university: string, subject: string, semesters: number, degree: string, "certificate-country": string, "certificate-canton": string, "certificate-place": string}} ChosenPreviousStudy
 */
